<nav class="navbar navbar-dark text-white border-bottom shadow-sm px-3" style="background-color: #6e1117; z-index: 999;">
    <div class="container-fluid d-flex justify-content-between align-items-center w-100">
        <!-- Left: Home Button -->
        @if (DeviceInfo.Current.Platform == DevicePlatform.WinUI)
        {
            <a class="btn btn-danger" href="/">Home</a>
        }

        <!-- Center: App Name -->
        <div class="flex-grow-1 text-center">
            <span class="navbar-brand mb-0 h1">Festival Mapper</span>
        </div>

        <!-- Right: Spacer or future content -->
        @if (DeviceInfo.Current.Platform == DevicePlatform.WinUI)
        {
            <div style="width: 68.69px;"></div>
        }
    </div>
</nav>

